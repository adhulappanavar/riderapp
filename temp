strapi.admin.services.auth.hashPassword(newPassword).then
(p => 
    {strapi.query(‘administrator’, ‘admin’)
        .update
        (
            { username: userName }, 
                { password: p }
        ); 
    }
);